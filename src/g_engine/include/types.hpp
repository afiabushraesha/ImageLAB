#ifndef _G_ENGINE_TYPES_H_
#define _G_ENGINE_TYPES_H_

namespace g_engine {
    template<typename T>
    struct vec2  { T x, y; };
    template<typename T>
    struct vec3  { T x, y, z; };
    template<typename T>
    struct vec4  { T x, y, z, w; };
}

#endif
